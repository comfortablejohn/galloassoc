import '../imports/api/tasks.js'
import { Meteor } from 'meteor/meteor';

// FlowRouter.route('/home', {
//     action: function() {
//         console.log("[FlowRouter] routing client to page: home");
//         render(<App pageName="home"/>, document.getElementById('render-target'));
//     },
//     name: "home"
// })

Meteor.startup(() => {
  // code to run on server at startup
});
